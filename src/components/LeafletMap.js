import React from 'react'
import styled from 'styled-components'

const MapContainer = styled.div`
  margin: auto;
  width: 50vw;
`

export const LeafletMap = () => {
  return <MapContainer>This is the map container</MapContainer>
}
